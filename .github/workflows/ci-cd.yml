name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run tests
      run: |
        pytest test_app.py -v

  security-scan:
      needs: test
      runs-on: ubuntu-latest
      
      steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install safety
        run: pip install safety
      
      - name: Safety scan with API key
        env:
          SAFETY_API_KEY: ${{ secrets.SAFETY_API_KEY }}
        run: safety scan --severity medium high critical
      
      - name: Run Bandit (SAST para Python)
        run: |
          pip install bandit
          bandit -r . -f json -o bandit-report.json || true
          cat bandit-report.json
          
  build:
    needs: [test, security-scan]
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Build Docker image
      run: |
        docker build -t pruebarepo:${{ github.sha }} .
        docker tag pruebarepo:${{ github.sha }} pruebarepo:latest
    
    - name: Test Docker image
      run: |
        docker run -d -p 5000:5000 --name test-container pruebarepo:latest
        sleep 5
        curl http://localhost:5000/health
        docker stop test-container
        docker rm test-container

  dast-scan:
    name: DAST con OWASP ZAP
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Build Docker image
      run: docker build -t pruebarepo:latest .
    
    - name: Start application container
      run: |
        docker run -d -p 5000:5000 --name app-container pruebarepo:latest
        echo "Waiting for app to start..."

    - name: Wait for app to be ready
      run: |
        timeout 30s bash -c 'until curl -f http://localhost:5000/health; do sleep 2; done'
        echo "✅ App is ready!"

    - name: Run OWASP ZAP Baseline Scan
      run: |
        mkdir -p $(pwd)/zap-reports
        chmod 777 $(pwd)/zap-reports
        docker run --network="host" \
          -v $(pwd)/zap-reports:/zap/wrk/:rw \
          -t ghcr.io/zaproxy/zaproxy:stable zap-baseline.py \
          -t http://localhost:5000 \
          -r zap-report.html \
          -J zap-report.json || true

    - name: Upload ZAP Reports
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: zap-reports
        path: zap-reports/
        
    - name: Stop application container
      if: always()
      run: |
        docker logs app-container
        docker stop app-container
        docker rm app-container
